server:
  port: 0
spring:
  application:
    name: analysis-service
  data:
    mongodb:
      database: tramways_projects

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URI:http://192.168.1.32:8761/eureka}
